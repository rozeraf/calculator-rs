name: Release

on:
  push:
    tags:
      - 'v2.*.*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }} (${{ matrix.crate }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # flash build (Linux with GMP system dependencies)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            crate: calc-flash
            use_cross: false
          # cross builds
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            crate: calc-cross
            use_cross: false
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            crate: calc-cross
            use_cross: true
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            crate: calc-cross
            use_cross: false
          - os: macos-latest
            target: x86_64-apple-darwin
            crate: calc-cross
            use_cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            crate: calc-cross
            use_cross: false

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2

      - name: Install GMP and MPFR (x86_64-unknown-linux-gnu only)
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgmp-dev libmpfr-dev libmpc-dev

      - name: Install musl-tools (x86_64 musl)
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get install -y musl-tools

      - name: Install cross
        if: matrix.use_cross == true
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build (cargo)
        if: matrix.use_cross == false
        run: cargo build --release -p ${{ matrix.crate }} --target ${{ matrix.target }}

      - name: Build (cross)
        if: matrix.use_cross == true
        run: cross build --release -p ${{ matrix.crate }} --target ${{ matrix.target }}

      - name: Build lipo universal (macOS only)
        if: matrix.os == 'macos-latest' && matrix.target == 'aarch64-apple-darwin'
        run: |
          # x86_64 was built in another job; here we build it again and lipo
          rustup target add x86_64-apple-darwin
          cargo build --release -p calc-cross --target x86_64-apple-darwin
          lipo -create \
            target/x86_64-apple-darwin/release/calc-cross \
            target/aarch64-apple-darwin/release/calc-cross \
            -output target/calc-universal
          tar czf calc-cross-universal-apple-darwin.tar.gz -C target calc-universal

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          # Set bin name based on crate
          if [ "${{ matrix.crate }}" = "calc-flash" ]; then
            BIN_NAME="calc-flash"
          else
            BIN_NAME="calc-cross"
          fi
          BIN=target/${{ matrix.target }}/release/$BIN_NAME
          tar czf ${{ matrix.crate }}-${{ matrix.target }}.tar.gz -C $(dirname $BIN) $(basename $BIN)

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: |
          $bin = "target\${{ matrix.target }}\release\calc-cross.exe"
          Compress-Archive -Path $bin -DestinationPath "${{ matrix.crate }}-${{ matrix.target }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.crate }}-${{ matrix.target }}
          path: |
            ${{ matrix.crate }}-${{ matrix.target }}.tar.gz
            ${{ matrix.crate }}-${{ matrix.target }}.zip
            calc-cross-universal-apple-darwin.tar.gz
          if-no-files-found: ignore

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: calc-*
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            calc-flash-*.tar.gz
            calc-cross-*.tar.gz
            calc-cross-*.zip
          generate_release_notes: true


